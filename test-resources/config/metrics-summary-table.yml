metricssummarytab:
  caption: Summarization of performance metrics where mF1 is the micro F1, mP is the
    micro precision, mR is the micro recall, MF1 is the macro F1, MP is the macro
    precision, MR is the macro recall.
  path: test-resources/csv/metrics-summary.csv
  type: two_column
  index_col_name: Id
  bold_cells: [[8, 2], [7, 5]]
  tabulate_params:
    disable_numparse: false
