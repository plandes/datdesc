irisRadarFig:
  image_dir: 'path: target'
  subplot_params:
    subplot_kw:
      projection: radar
  plots:
    - type: radar
      data: "dataframe({'index_col': 'species'}): test-resources/fig/iris.csv"
      title: 'Iris Data'
      key_title: 'Key'
      code_pre: |
        df = plot.data
        df = df.loc[~df.index.duplicated(keep='first'), :]
        df = df[df.columns[:-1]]
        plot.data = df
